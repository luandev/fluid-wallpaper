# Production validation

## Environment and repository checks

Capture session: 2026-09-17, Windows, Node 24.15.0. Chrome 152 through ANGLE / Direct3D11 on NVIDIA GeForce RTX 5070 Ti. Exact user agent, GL renderer and viewport are retained in each shot JSON under ignored `promo-output/`.

`yarn test`: 118 tests passed across 18 files. `yarn build`: TypeScript, Vite site/package builds and package export/allowlist/SSR checks passed. Initial runs found missing Windows launchers for existing installed modules; temporary launchers outside the repository resolved that without dependency installation or lockfile edits.

The 45-frame smoke capture encoded 1920×1080, 30 fps H.264 and displayed actual WebGL output. No shader error was reported. The initial sandboxed Chrome attempt exited; authorized execution outside the sandbox succeeded. This records the capture environment, not a mobile or desktop host certification.

The microphone step recorded 360 analyser samples with band values spanning 0–1. The WAV used was generated by this workflow. Offline replay semantics and soundtrack alignment are described in [README.md](README.md#capture-semantics-and-evidence-boundaries).

## Final media review

Final export checks and visual review are recorded here after production completes. Generated detailed evidence lives in `promo-output/validation.json`, decode logs and per-shot JSON files.
